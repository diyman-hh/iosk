# 🔗 Mac Xcode 访问 Windows 代码指南

## 方案对比

| 方案 | 描述 | 优点 | 缺点 | 推荐指数 |
| :--- | :--- | :--- | :--- | :--- |
| **SMB 共享** | Windows 开局域网共享，Mac 直接挂载 | 无需上传下载，实时修改 | 编译速度受网络影响，可能卡顿 | ⭐⭐⭐ |
| **Git 同步** | Windows 推送代码，Mac 拉取代码 | 编译速度快，版本安全 | 需要手动 Push/Pull | ⭐⭐⭐⭐⭐ |

---

## 方案 1：局域网共享 (SMB) - 详细步骤

### 1. Windows 端设置 (服务器)

1.  **定位文件夹**：找到 `d:\project\iosk`。
2.  **开启共享**：
    *   右键文件夹 > **属性** > **共享** > **高级共享**。
    *   勾选 **"共享此文件夹"**。
    *   点击 **"权限"** > 选中用户 > 勾选 **"完全控制"** (Read/Write)。
    *   一路点击 **"确定"** 保存。
3.  **获取 IP 地址**：
    *   `Win + R` > 输入 `cmd` > 输入 `ipconfig`。
    *   记录 **IPv4 地址** (例如 `192.168.31.100`)。
    *   *注意：Windows 和 Mac 必须连接同一个 WiFi 或路由器。*

### 2. Mac 端连接 (客户端)

1.  **连接服务器**：
    *   打开 **Finder (访达)**。
    *   快捷键 `Command + K`。
    *   输入地址：`smb://你的WindowsIP` (例如 `smb://192.168.31.100`)。
    *   点击 **连接**。
2.  **身份验证**：
    *   输入你的 **Windows 电脑用户名** 和 **密码**。
    *   如果 Windows 使用的是微软账号登录，用户名通常是微软邮箱，或者是 `C:\Users\` 下的文件夹名。
3.  **挂载**：
    *   选择刚才共享的文件夹 (`iosk`)。
    *   现在这个文件夹就像 Mac 的一个U盘一样了。

### 3. Xcode 使用

1.  启动 Xcode。
2.  **Open a project or file**。
3.  在左侧选择共享的磁盘/服务器，找到项目文件 (`.xcodeproj` 或 `Makefile`)。
    *   *注意：由于这是一个 Theos 项目，没有 `.xcodeproj`，你需要创建或使用命令行编译，或者使用 Xcode 打开文件夹作为文件编辑器。*

---

## 方案 2：Git 同步 (推荐)

这是最专业的开发方式，避免网络延迟导致的编译错误。

1.  **Windows**: 修改代码 -> `git push`。
2.  **Mac**: 打开终端 -> `git pull` -> 使用 Xcode/Make 编译。

这样编译是在 Mac 本地硬盘进行的，速度最快且最稳定。
