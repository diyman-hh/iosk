name: Build TrollTouch (Dual IPA)

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      # ========================================
      # Build TrollTouchAgent (Agent IPA)
      # ========================================
      
      - name: Generate Agent Xcode Project
        run: |
          cd TrollTouchAgent
          xcodegen generate
          echo "✅ Agent Xcode project generated"

      - name: Build TrollTouchAgent
        run: |
          cd TrollTouchAgent
          echo "🔨 Building TrollTouchAgent..."
          set -o pipefail
          xcodebuild -project TrollTouchAgent.xcodeproj \
                     -scheme TrollTouchAgent \
                     -sdk iphoneos \
                     -configuration Release \
                     -derivedDataPath ./build \
                     CODE_SIGNING_ALLOWED=NO \
                     CODE_SIGN_IDENTITY="" \
                     CODE_SIGN_ENTITLEMENTS="TrollTouchAgent/TrollTouchAgent.entitlements" \
                     PRODUCT_BUNDLE_IDENTIFIER=com.trolltouch.agent \
                     IPHONEOS_DEPLOYMENT_TARGET=14.0 \
                     2>&1 | tee build.log
          
          echo "✅ Agent build completed"

      - name: Package Agent IPA
        run: |
          cd TrollTouchAgent
          echo "📦 Packaging Agent IPA..."
          
          APP_PATH="./build/Build/Products/Release-iphoneos/TrollTouchAgent.app"
          
          if [ ! -d "$APP_PATH" ]; then
            echo "❌ Agent app not found at $APP_PATH"
            exit 1
          fi
          
          # Create Payload directory
          mkdir -p Payload
          cp -r "$APP_PATH" Payload/
          
          # Create IPA
          zip -r TrollTouchAgent.ipa Payload
          
          echo "✅ Agent IPA created"
          ls -lh TrollTouchAgent.ipa

      # ========================================
      # Build TrollTouch (Main IPA)
      # ========================================

      - name: Generate Main App Xcode Project
        run: |
          xcodegen generate
          echo "✅ Main app Xcode project generated"

      - name: Build TrollTouch
        run: |
          echo "🔨 Building TrollTouch..."
          set -o pipefail
          xcodebuild -project TrollTouch.xcodeproj \
                     -scheme TrollTouch \
                     -sdk iphoneos \
                     -configuration Release \
                     -derivedDataPath ./build \
                     CODE_SIGNING_ALLOWED=NO \
                     CODE_SIGN_IDENTITY="" \
                     CODE_SIGN_ENTITLEMENTS="TrollTouch/TrollTouch.entitlements" \
                     PRODUCT_BUNDLE_IDENTIFIER=com.trolltouch.app \
                     IPHONEOS_DEPLOYMENT_TARGET=14.0 \
                     2>&1 | tee build.log
          
          echo "✅ Main app build completed"

      - name: Package Main IPA
        run: |
          echo "📦 Packaging Main IPA..."
          
          APP_PATH="./build/Build/Products/Release-iphoneos/TrollTouch.app"
          
          if [ ! -d "$APP_PATH" ]; then
            echo "❌ Main app not found at $APP_PATH"
            exit 1
          fi
          
          # Create Payload directory
          mkdir -p Payload
          cp -r "$APP_PATH" Payload/
          
          # Create IPA
          zip -r TrollTouch.ipa Payload
          
          echo "✅ Main IPA created"
          ls -lh TrollTouch.ipa

      # ========================================
      # Create Release Package
      # ========================================

      - name: Create Release Package
        run: |
          echo "📦 Creating release package..."
          
          mkdir -p release
          cp TrollTouchAgent/TrollTouchAgent.ipa release/
          cp TrollTouch.ipa release/
          
          # Create README
          cat > release/README.txt << 'EOFMARKER'
          TrollTouch - Dual IPA Package
          ==============================
          
          Installation Steps:
          1. Install TrollTouchAgent.ipa using TrollStore
          2. Install TrollTouch.ipa using TrollStore
          3. Open TrollTouchAgent first (runs HTTP Server in background)
          4. Then open TrollTouch (main app)
          
          Notes:
          - TrollTouchAgent must keep running for touch injection
          - Both apps need TrollStore installation for necessary permissions
          - Requires iOS 14.0 or higher
          EOFMARKER
          
          # Create zip
          TIMESTAMP=$(date +%Y%m%d-%H%M%S)
          cd release
          zip -r ../TrollTouch-Complete-${TIMESTAMP}.zip .
          cd ..
          
          echo "✅ Release package created"
          ls -lh TrollTouch-Complete-*.zip

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: TrollTouch-IPAs
          path: |
            TrollTouch-Complete-*.zip
            release/TrollTouchAgent.ipa
            release/TrollTouch.ipa
            release/README.txt

      - name: Build Summary
        run: |
          echo "## 🎉 Build Successful!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Generated Files:" >> $GITHUB_STEP_SUMMARY
          echo "- TrollTouchAgent.ipa (Agent - 触摸注入服务)" >> $GITHUB_STEP_SUMMARY
          echo "- TrollTouch.ipa (Main App - 用户界面)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📥 Download:" >> $GITHUB_STEP_SUMMARY
          echo "Check the 'Artifacts' section above to download the complete package." >> $GITHUB_STEP_SUMMARY
